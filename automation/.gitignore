# ─── Secrets — NEVER commit these ────────────────────────────────────────────
.env
.env.*
!.env.example
credentials.json
token.json
employees.json
state.json
state-*.json
push-channels.json
gmail-state.json
seen-files.json
seen-files-meta.json

# ─── Dependencies ─────────────────────────────────────────────────────────────
node_modules/
package-lock.json

# ─── Runtime / OS ─────────────────────────────────────────────────────────────
*.log
logs/
.DS_Store
Thumbs.db

# ─── Google Apps Scripts (local only — run manually via script.google.com) ────
scripts/*.gs

# ─── ngrok ────────────────────────────────────────────────────────────────────
../ngrok.exe
../ngrok.zip
../ngrok
ngrok.exe
ngrok.zip
ngrok
